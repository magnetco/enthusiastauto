export default {
  theme: {
    container: {
      center: true,
      padding: { DEFAULT: 'var(--page-x)', lg: 'var(--page-x-lg)' },
      screens: {
        '2xl': 'var(--container-max)'
      }
    }
  }
} as const;
